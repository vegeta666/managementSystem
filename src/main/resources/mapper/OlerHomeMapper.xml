<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duan.system.dao.OlderHomeMapper">
    <insert id="insert" parameterType="olderHome">
        insert into older_home(olderid, main, mainNum, mainRelationship, second, secondNum, secondRelationship)
        values (#{olderid},#{main},#{mainNum},#{mainRelationship},#{second},#{secondNum},#{secondRelationship})
    </insert>
</mapper>