<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duan.system.dao.OlderCostMapper">
    <insert id="insert" parameterType="olderCost">
        insert into older_cost(olderid,shouldCost,actualCost,arrears)
        values (#{olderid},#{shouldCost},#{actualCost},#{arrears})
    </insert>
</mapper>